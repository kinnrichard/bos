// Wrapper to make Sortable.js work with ES modules
// First, include the UMD build inline or load it
import 'sortable.min';

// Export the global Sortable that was created by the UMD build
export default window.Sortable;